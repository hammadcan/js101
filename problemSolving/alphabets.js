let wordBlock = ['BO', 'XK', 'DQ', 'CP', 'NA', 'GT', 'RE', 'FS', 'JW', 'HU', 'VI', 'LY', 'ZM'];

function isBlockWord(string) {
  let letters = string.split('');
  for (let counter = 0; counter < letters.length; counter++) {
    if ()
  }
}